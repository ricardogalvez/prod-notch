.site-header {

  background: green;

  &__logo-wrapper {

     h5 {

       a {
         font-size: 3.6(rem);
         letter-spacing: 0.1(rem);
         padding: 1(rem) 0 0 0;
         top: 4(rem);
         // stuff
       }
     }
  } // /__logo-wrapper

  &__logo-image {
    float: left;
    height: 60px;
    position: relative;
    top: 0.6rem;
  }
}
